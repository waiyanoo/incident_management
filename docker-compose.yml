version: '2'
services:
  ui:
    build: ./ui
    ports:
      - '8080:8080'
    depends_on:
      - api
  api:
    build: ./api
    ports:
      - '5000:5000'
    depends_on:
      - mongo
  mongo:
    image: mongo
    ports:
      - '27017:27017'